[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    smoke: Quick smoke tests
    data: Data validation tests
    artifacts: Artifact validation tests
    quality_gate: Quality gate tests
